/** @type {import('tailwindcss').Config} */
export default {
  content: [
    './index.html', // if index.html is present in the root
    './src/**/*.{vue,js,ts,jsx,tsx}', // for all Vue, JS, TS, JSX, and TSX files in src folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
